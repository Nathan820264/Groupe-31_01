mdp = input("Entrez un mot de passe : ")
if len(mdp) >= 8 and any(c.isdigit() for c in mdp) and any(c.isupper() for c in mdp):
    print("Not de passe valide.")
else:
    print("Not de passe invalide.")